name: Queue Processor

on:
  # Schedule disabled while testing locally. Re-enable by uncommenting the cron line below.
  # schedule:
  #   - cron: '*/10 13-23 * * 1-5'
  workflow_dispatch:

jobs:
  ping-queue:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Webhook
        run: curl -s -X POST ${{ secrets.CRON_URL }}
